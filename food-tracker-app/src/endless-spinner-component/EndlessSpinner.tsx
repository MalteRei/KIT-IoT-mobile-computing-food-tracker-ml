import React from 'react';
import './EndlessSpinner.css';

const EndlessSpinner: React.FunctionComponent = (props) => {
    return (
        <div className="spinner-container">
            <div className="spinner-circle">

            </div>
        </div>
    );
}

export default EndlessSpinner;